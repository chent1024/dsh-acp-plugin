# The harness peers (@deepseek-ai/*) are supplied by the running profile, not
# installed by this package: a profile sets `autoInstallPeers: false` and the
# harness tree provides them. Auto-installing them here would pull published
# versions that need not match the harness actually running the plugin.
auto-install-peers=false
